Description:
    Creates a default Assemble app

Options:
    --skip-install: Skips the automatic execution of `bower` and `npm`
      after scaffolding has finished.
    --silent: Skips question and welcome message.

Example:
    yo assemble [--skip-install] [--silent]
